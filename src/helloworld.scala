object helloworld {
  def main(args: Array[String]): Unit = {
    /*
  val List(_,_,th,fo,rest @ _*) = List(1,2,3,4,5)
  println(th)
  println(rest)

    def printnumber(func:Int => String):Unit = {
      var acc = 0
      while (acc <10) {
        println(func(acc))
        acc = acc + 1
      }
    }

    def func(num:Int):String={
       s"${Console.MAGENTA_B}$num${Console.RESET}"
    }
    printnumber(func)
    println(
      List(1,2,3).sortWith((a,b)=>a>b)
    )
    val melon = Array("watermelon","lemon")
    melon.update(1,"supermelon")
    melon.foreach(println)
    */

  }
}
